extends base

block content
  div(id="#{uid}")
    | !{html}
  script
    | !{state}
  if devPort
    script(src="http://localhost:#{devPort}/build/bundle.js")
  else
    script(src="/client.#{hash}.js")
